pipeline {
    agent {
        label 'librecores/librecores-ci-modules'
    }
    
    stages {
      // Run the existing Test suite
      stage("Test") {
        steps {
          sh "ls /tools"
        }
      }
    }
}
