pipeline {
    agent { docker 'librecores/librecores-ci-modules' }
    
    stages {
      // Run the existing Test suite
      stage("Test") {
        steps {
          sh "ls"
        }
      }
    }
}
